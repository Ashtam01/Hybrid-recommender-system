# ===== DVC-tracked raw data (pointer .dvc files ARE in git) =====
/Music Info.csv
/User Listening History.csv

# ===== DVC pipeline outputs (generated by `dvc repro`) =====
# These are defined as `outs:` in dvc.yaml — DVC tracks them, not Git
/cleaned_data.csv
/transformed_data.npz
/collab_filtered_data.csv
/interaction_matrix.npz
/track_ids.npy
/transformed_hybrid_data.npz
